# TalkArena Architecture Diagram

Use unified architecture diagram file:

- `docs/talkarena_unified_uml.mmd`

This diagram includes multimodal paths (text + mic + camera signal), model routing, agent runtime, and serial voice playback.
